// NOTE: Action names are limited to 12 characters, otherwise Trystero breaks.
// ^ Thanks to Jeremy Kahn at ChitChatter
export enum PeerActions {
  MESSAGE = "MESSAGE",
  MESSAGE_TRANSCRIPT = "MSG_XSCRIPT",
  PEER_METADATA = "PEER_META",
  PEER_TYPE = "PEER_TYPE",
}
